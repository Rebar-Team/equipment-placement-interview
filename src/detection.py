"""Part 1: Floor Plan Detection"""

from typing import List
from .models import Rect


def detect_tags(image_path: str) -> List[Rect]:
    """
    Detect placement zones in a floor plan image.

    Args:
        image_path: Path to a floor plan PNG file.

    Returns:
        List of Rect objects, one per detected zone.

    TODO: Implement this function.
    """
    raise NotImplementedError("Implement detect_tags()")
